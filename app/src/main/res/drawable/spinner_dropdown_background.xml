<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@android:color/transparent" />  <!-- fixes white edges -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="?attr/colorSurface" />
            <corners android:radius="12dp" />
            <stroke android:width="1dp" android:color="?attr/colorOutline" />
            <shadowColor android:color="@android:color/black" />
            <shadowDx android:value="0" />
            <shadowDy android:value="2" />
            <shadowRadius android:value="4" />
        </shape>
    </item>
</layer-list>
